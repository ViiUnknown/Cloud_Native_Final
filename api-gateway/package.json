{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "API Gateway - single entry point: JWT auth, RBAC, rate limiting, round-robin load balancing, proxying",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "http-proxy": "^1.18.1",
    "jsonwebtoken": "^9.0.2"
  }
}
